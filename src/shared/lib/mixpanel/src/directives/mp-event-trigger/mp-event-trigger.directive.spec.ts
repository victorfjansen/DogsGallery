import { MpEventTriggerDirective } from './mp-event-trigger.directive';

describe('MpEventTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new MpEventTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
